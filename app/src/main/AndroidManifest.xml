<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.sispakhukumkesehatan">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
                android:name=".activity.HasilIdentifikasiActivity"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.KonsultasiActivity"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.HasilKonsultasiActivity"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.IdentifikasiActivity"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.Bantuan"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.TentangAplikasi"
                android:theme="@style/AppThemeDark" />
        <activity
                android:name=".activity.UUPdfViewer"
                android:theme="@style/AppThemeDark" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>